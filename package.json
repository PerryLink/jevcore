{
  "name": "dsh-jev-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "TypeSafe Jev for DeepSeek Harness: a framework-agnostic decision core and the DSH plugin built on it.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "legal": "node scripts/sync-legal-files.mjs --check",
    "check": "pnpm run legal && pnpm run typecheck && pnpm run test && pnpm run build",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
