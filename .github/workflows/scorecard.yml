# OpenSSF Scorecard supply-chain security analysis (dimension C of
# dsh-plugin-certification). Publishes results to api.securityscorecards.dev.
#
# No secret is read here: publishing uses the workflow's OIDC token
# (`id-token: write`), not a repository secret.
name: Scorecard supply-chain security
on:
  push:
    branches: ["main"]
  schedule:
    - cron: '23 4 * * 6'
  workflow_dispatch:

permissions: read-all

jobs:
  analysis:
    name: Scorecard analysis
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      id-token: write
    steps:
      - name: "Checkout code"
        # Pinned to the major the other workflows in this repository use.
        uses: actions/checkout@v5
        with:
          persist-credentials: false
      - name: "Run analysis"
        uses: ossf/scorecard-action@v2.4.4
        with:
          results_file: results.sarif
          results_format: sarif
          publish_results: true
      - name: "Upload artifact"
        uses: actions/upload-artifact@v7
        with:
          name: SARIF file
          path: results.sarif
          retention-days: 5
      - name: "Upload to code-scanning"
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif
